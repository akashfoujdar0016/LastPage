import {Router} from 'express'; import {z} from 'zod'; import {db} from '../db/mongoose.js'; import {auth,requireRole} from '../middleware/core.js'; import {User,Follow,Review,ReviewLike,Comment,Activity,Notification,Report} from '../models/index.js'; import {notify} from '../services/social.js';
const r=Router();
r.post('/users/:id/follow',auth(),async(req,res,next)=>{try{await db();if(req.params.id===req.user?.sub)return res.status(400).json({error:'Cannot follow yourself'});const target=await User.findById(req.params.id);if(!target)return res.status(404).json({error:'User not found'});const f=await Follow.findOne({followerId:req.user?.sub,followingId:target._id});if(f){await f.deleteOne();return res.json({following:false});}await Follow.create({followerId:req.user?.sub,followingId:target._id});await notify(target._id,req.user?.sub,'FOLLOW');res.json({following:true});}catch(e){next(e)}});
r.get('/users/:username',auth(false),async(req,res,next)=>{try{await db();const u=await User.findOne({username:req.params.username.toLowerCase()}).select('-passwordHash').lean();if(!u)return res.status(404).json({error:'User not found'});if(u.privacy?.profile==='PRIVATE'&&String(u._id)!==req.user?.sub)return res.status(403).json({error:'Private profile'});const [followers,following,reviews]=await Promise.all([Follow.countDocuments({followingId:u._id}),Follow.countDocuments({followerId:u._id}),Review.countDocuments({userId:u._id,deletedAt:null})]);res.json({user:u,stats:{followers,following,reviews}});}catch(e){next(e)}});
r.post('/reviews/:id/like',auth(),async(req,res,next)=>{try{await db();const rv=await Review.findById(req.params.id);if(!rv)return res.status(404).json({error:'Review not found'});const x=await ReviewLike.findOne({userId:req.user?.sub,reviewId:rv._id});if(x){await x.deleteOne();await Review.updateOne({_id:rv._id},{$inc:{likeCount:-1}});return res.json({liked:false});}await ReviewLike.create({userId:req.user?.sub,reviewId:rv._id});await Review.updateOne({_id:rv._id},{$inc:{likeCount:1}});await notify(rv.userId,req.user?.sub,'REVIEW_LIKE',rv._id);res.json({liked:true});}catch(e){next(e)}});
r.post('/reviews/:id/comments',auth(),async(req,res,next)=>{try{await db();const x=z.object({body:z.string().min(1).max(2000)}).parse(req.body);const rv=await Review.findById(req.params.id);if(!rv)return res.status(404).json({error:'Review not found'});const c=await Comment.create({userId:req.user?.sub,reviewId:rv._id,body:x.body});await Review.updateOne({_id:rv._id},{$inc:{commentCount:1}});await notify(rv.userId,req.user?.sub,'COMMENT',rv._id);res.status(201).json({comment:c});}catch(e){next(e)}});
r.get('/feed',auth(),async(req,res,next)=>{try{await db();const following=await Follow.find({followerId:req.user?.sub}).distinct('followingId');const items=await Activity.find({userId:{$in:[req.user?.sub,...following]}}).sort({createdAt:-1}).limit(50).populate('userId','username displayName avatarUrl').lean();res.json({items});}catch(e){next(e)}});
r.get('/notifications',auth(),async(req,res,next)=>{try{await db();const items=await Notification.find({userId:req.user?.sub}).sort({createdAt:-1}).limit(50).populate('actorId','username displayName avatarUrl').lean();res.json({items});}catch(e){next(e)}});
r.post('/reports',auth(),async(req,res,next)=>{try{await db();const x=z.object({targetType:z.string().min(1),targetId:z.string(),reason:z.string().min(3).max(1000)}).parse(req.body);const report=await Report.create({reporterId:req.user?.sub,targetType:x.targetType,targetId:x.targetId,reason:x.reason});res.status(201).json({report});}catch(e){next(e)}});
r.get('/reports',auth(),requireRole('MODERATOR','ADMIN'),async(req,res,next)=>{try{await db();res.json({items:await Report.find().sort({createdAt:-1}).limit(100).lean()});}catch(e){next(e)}});
export default r;
