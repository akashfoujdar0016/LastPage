import {Router} from 'express'; import {db} from '../db/mongoose.js'; import {auth} from '../middleware/core.js'; import {Content,Rating,Like,Favorite,Status} from '../models/index.js';
const r=Router();
r.get('/home',auth(false),async(req,res,next)=>{try{await db();const [trending,popular,rated,movies,books]=await Promise.all([Content.find({deletedAt:null}).sort({popularity:-1}).limit(12).lean(),Content.find({deletedAt:null}).sort({likeCount:-1}).limit(12).lean(),Content.find({deletedAt:null,ratingCount:{$gte:3}}).sort({averageRating:-1}).limit(12).lean(),Content.find({type:'MOVIE',deletedAt:null}).sort({year:-1}).limit(12).lean(),Content.find({type:'BOOK',deletedAt:null}).sort({year:-1}).limit(12).lean()]);res.json({trending,popular,topRated:rated,movies,books});}catch(e){next(e)}});
r.get('/recommendations',auth(),async(req,res,next)=>{try{await db();const rated=await Rating.find({userId:req.user?.sub}).sort({updatedAt:-1}).limit(30).lean();const liked=await Like.find({userId:req.user?.sub}).limit(30).lean();const seen=[...rated.map(x=>x.contentId),...liked.map(x=>x.contentId)];const preferred=await Content.find({_id:{$in:rated.filter(x=>x.score>=4).map(x=>x.contentId)}}).lean();const genres=[...new Set(preferred.flatMap(x=>x.genres||[]))];const items=await Content.find({deletedAt:null,_id:{$nin:seen},genres:{$in:genres}}).sort({popularity:-1,averageRating:-1}).limit(24).lean();res.json({items,signals:{genres}});}catch(e){next(e)}});
export default r;
