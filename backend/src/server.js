import express from 'express'; import {db} from './db/mongoose.js'; import {env} from './config/env.js'; import {security} from './middleware/core.js'; import auth from './routes/auth.js'; import content from './routes/content.js'; import social from './routes/social.js'; import library from './routes/library.js'; import discovery from './routes/discovery.js';
const app=express(); app.set('trust proxy',1); app.use(...security); app.use(express.json({limit:'1mb'})); app.get('/api/health',async(_,res)=>{try{await db();res.json({ok:true,service:'cinefolio-api',db:'connected'});}catch{res.status(503).json({ok:false,db:'unavailable'});}}); app.use('/api/auth',auth);app.use('/api/content',content);app.use('/api/social',social);app.use('/api/me',library);app.use('/api/discovery',discovery);app.use((err,_req,res,_next)=>{ console.error(err); const status = err?.name==='ZodError'?400:500; const message = err?.message; res.status(status).json({ error: err?.name==='ZodError'?'Invalid request':'Internal server error', details: env.NODE_ENV==='production'?undefined:message }); });
if(process.env.VERCEL!=='1')app.listen(env.PORT,()=>console.log(`API on ${env.PORT}`)); export default app;
