'use client'; import {useEffect,useState} from 'react'; import {api} from '../../lib/api'; export default function Library(){const [items,setItems]=useState([]);useEffect(()=>{api('/me').then(x=>setItems(x.items)).catch(()=>{})},[]);return <div className="shell py-12"><h1 className="text-5xl font-black">My library</h1><div className="grid md:grid-cols-3 gap-4 mt-10">{items.map((x)=><div className="card p-5" key={x._id}><div className="text-xs muted">{x.content?.type}</div><div className="font-bold mt-1">{x.content?.title}</div><div className="text-sm mt-2">{x.status}</div></div>)}</div></div>}
