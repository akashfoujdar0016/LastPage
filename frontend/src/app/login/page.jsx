'use client'; import {useState} from 'react'; import {login} from '../../lib/api'; export default function Login(){const [email,setEmail]=useState('demo@example.com'),[password,setPassword]=useState('password123'),[msg,setMsg]=useState('');return <div className="shell py-20 max-w-md"><h1 className="text-4xl font-black">Welcome back</h1><p className="muted mt-2">Sign in to your diary.</p><input className="input mt-8" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input mt-3" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="btn btn-primary w-full mt-4" onClick={async()=>{try{await login(email,password);location.href='/'}catch(e){setMsg(e.message)}}}>Sign in</button>{msg&&<p className="text-sm mt-3">{msg}</p>}</div>}
