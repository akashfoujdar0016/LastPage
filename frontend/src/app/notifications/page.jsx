'use client'; import {useEffect,useState} from 'react'; import {api} from '../../lib/api'; export default function Notifications(){const [items,setItems]=useState([]);useEffect(()=>{api('/social/notifications').then(x=>setItems(x.items)).catch(()=>{})},[]);return <div className="shell py-12 max-w-3xl"><h1 className="text-5xl font-black">Notifications</h1><div className="space-y-3 mt-8">{items.map((x)=><div className="card p-5" key={x._id}><b>{x.actorId?.displayName||x.actorId?.username||'Someone'}</b> {x.type.toLowerCase().replaceAll('_',' ')}<div className="text-xs muted mt-2">{new Date(x.createdAt).toLocaleString()}</div></div>)}</div></div>}
