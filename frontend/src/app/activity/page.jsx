'use client'; import {useEffect,useState} from 'react'; import {api} from '../../lib/api'; export default function Activity(){const [items,setItems]=useState([]);useEffect(()=>{api('/social/feed').then(x=>setItems(x.items)).catch(()=>{})},[]);return <div className="shell py-12"><h1 className="text-5xl font-black">Activity</h1><div className="space-y-3 mt-8">{items.map((x)=><div className="card p-5" key={x._id}><b>{x.userId?.displayName||x.userId?.username}</b> <span className="muted">{x.type.toLowerCase().replaceAll('_',' ')}</span><div className="text-xs muted mt-2">{new Date(x.createdAt).toLocaleString()}</div></div>)}</div></div>}
